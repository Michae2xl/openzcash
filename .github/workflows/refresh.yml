name: Refresh ZCG data
on:
  workflow_dispatch:
  schedule:
    - cron: "23 6 * * *"
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://openzcash.org/api/cron/refresh
